import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.william.tarea2',
  appName: 'tarea2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
